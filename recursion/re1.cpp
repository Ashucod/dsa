#include<bits/stdc++.h>

int main() {
    // What is recursion?
    // when a function calls itself until a specific condition is fullfilled
    // Such condition is known as base condition
    // Stack overflow $ Stack space - it is the space where yet to be executed functions are stored.
    // Recursion tree - visual representation in terms of f(). 
    return 0;
}